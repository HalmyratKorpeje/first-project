public  class classwork {
    public string compareSets (set <Integer> setName = new set <Integer>();){
        string b = '';
        
        setName.add(222);
        setName.add(333);
        setName.add(111);
        if (setName.equals(a,e,c)){
            b = 'both sets are identical';
        }
        else {
            b = 'both sets are not identical';
        }
    return b;

    }
}
